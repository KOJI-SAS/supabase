apiVersion: v1
kind: Service
metadata:
  name: {{ include "supabase-realtime.fullname" . }}
  labels:
    {{- include "supabase-realtime.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "supabase-realtime.selectorLabels" . | nindent 4 }}
