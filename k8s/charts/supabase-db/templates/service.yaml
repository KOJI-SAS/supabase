apiVersion: v1
kind: Service
metadata:
  name: {{ include "supabase-db.fullname" . }}
  labels:
    {{- include "supabase-db.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "supabase-db.selectorLabels" . | nindent 4 }}
